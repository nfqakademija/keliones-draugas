{% extends 'base.html.twig' %}

{% block title %}Edit CoordinateType{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="m-4">Edit Coordinate type</h1>

        {{ include('coordinate_type/_form.html.twig', {'button_label': 'Update'}) }}

        <a class='btn btn-primary m-3' href="{{ path('coordinate_type_index') }}">Back to list</a>

        {{ include('coordinate_type/_delete_form.html.twig') }}
    </div>
{% endblock %}